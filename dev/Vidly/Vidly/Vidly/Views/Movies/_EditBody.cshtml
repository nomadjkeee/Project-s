
@*@using Vidly.ViewModel
@model FilmTemplateViewModel
@using (Html.BeginForm("Create", "Movies", FormMethod.Post))
{
    @Html.ValidationSummary()
    <div class="form-group">
        @Html.HiddenFor(m => m.Movies.Id)
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Movies.Name)
        @Html.TextBoxFor(m => m.Movies.Name, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Movies.Name)

    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Movies.ReleaseDate)
        @Html.TextBoxFor(m => m.Movies.ReleaseDate, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Movies.ReleaseDate)
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Movies.GenreId)
        @Html.DropDownListFor(m => m.Movies.GenreId, new SelectList(Model.Genres, "Id", "GenreType"), Model.Movies == null ? "Select some Genre" : Model.Genres.First(m => m.Id == Model.Movies.GenreId).GenreType, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Movies.GenreId)
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Movies.Year)
        @Html.TextBoxFor(m => m.Movies.Year, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Movies.Year)
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Movies.Detail)
        @Html.TextBoxFor(m => m.Movies.Detail, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Movies.Detail)
    </div>

    <div class="form-group">
        <button type="submit" style="width:20%;height:20%" class="btn-primary">Add Movie</button>
    </div>
}*@
